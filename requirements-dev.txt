# Test-only dependencies. Install alongside each service's runtime deps:
#   pip install -r requirements-dev.txt \
#               -r services/ingestion/requirements.txt \
#               -r services/discord-bot/requirements.txt \
#               -r services/rag/requirements.txt
pytest>=8,<10
pytest-cov>=5,<8

# Integration suite (tests/integration/) — requires Docker. testcontainers
# owns the pgvector container, httpx is the FastAPI TestClient transport.
testcontainers[postgres]>=4,<5
httpx>=0.27,<1
